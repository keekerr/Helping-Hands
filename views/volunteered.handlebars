<div class="flex mb-4"><!--whole container-->

<h2>you have volunteered for these events</h2>  
    
    <div class="w-1/2 bg-gradient-to-r from-yellow-900 via-blue-200 to-yellow-800 flex flex-col items-center">
        <!--volunteer event-->
        {{#each events as |event|}}
        {{!-- TODO: Put back eq --}}

        {{!-- {{#if (eq event.user.volunteerEvent_id user.volunteerEvent_id)}} --}}
        {{>volevent}}
        {{!-- {{/if}} --}}
        {{/each}}


    </div>
</div>