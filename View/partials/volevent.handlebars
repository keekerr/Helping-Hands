<section class="eventcard"> 
        <div class="vol_event_card">  
    <header class="pg_topper"> volunteer events:</header> 
       <div class="event_header">
        <p>{{volunteer.event.event_name}} on {{ format_date volunteer.event.event_date}} is a {{volunteer.event.event_type}} event</p>
    </div>
    <div class="event_description">
        <p> {{volunteer.event_description}} </p>
        <p> {{volunteer.event.event_address}}, {{volunteer.event.event_state}} {{volunteer.event_zip}} </p>
    </div>
        </div>

    </section>

    <!-- hang to the right side of page 
        <div class="card text-white MAKE DARK PURPLE mb-3" style="max-width: 18rem;">
  <div class="card-header">{{volunteerevent.name}}</div>
  <div class="card-body">
    <h5 class="card-title">{{user.name}}</h5>
    <p class="card-text">{{volunteer.description}}</p>
  </div>
</div>
-->